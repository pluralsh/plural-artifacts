apiVersion: plural.sh/v1alpha1
kind: Dependencies
metadata:
  description: ray aws setup
  version: 0.1.4
spec:
  dependencies:
  - name: aws-bootstrap
    repo: bootstrap
    type: terraform
    version: '>= 0.1.51'
  providers:
  - aws
