apiVersion: v1
kind: Service
metadata:
  name: {{ include "dash-controller.fullname" . }}
  labels:
    {{- include "dash-controller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dash-controller.selectorLabels" . | nindent 4 }}
