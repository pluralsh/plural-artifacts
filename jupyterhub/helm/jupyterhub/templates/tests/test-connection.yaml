apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jupyterhub.fullname" . }}-test-connection"
  labels:
    {{- include "jupyterhub.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jupyterhub.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
