name: velero-aws
description: Installs velero on an aws eks cluster
provider: AWS
primary: false
private: true
dependencies:
- repo: bootstrap
  name: aws-k8s
sections:
- name: velero
  configuration:
  - name: velero_bucket
    type: BUCKET
    default: velero
    documentation: bucket to store velero data
  items:
  - type: TERRAFORM
    name: aws
  - type: HELM
    name: velero
