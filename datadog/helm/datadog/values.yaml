# Default values for datadog.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

config-overlays:
  configOverlays:
    site-url:
      spec:
        name: Site Url
        documentation: whether you need to enable a unique datadog site url
        updates:
        - path: ['datadog', 'datadog', 'datadog', 'site']