name: etcd-gcp
description: Installs etcd on a gcp gke cluster
provider: GCP
dependencies:
- repo: bootstrap
  name: gcp-kubernetes
sections:
- name: etcd
  items:
  - type: TERRAFORM
    name: gcp
    configuration: []
  - type: HELM
    name: etcd
    configuration: []
