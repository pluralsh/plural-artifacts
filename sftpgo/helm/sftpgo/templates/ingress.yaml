apiVersion: v1
kind: Secret
metadata:
  name: sftpgo.plural-postgres-sftpgo.credentials.postgresql.acid.zalan.do
  labels:
{{ include "sftpgo-plural.labels" . | indent 4 }}
stringData:
  username: {{ .Values.postgres.user }}
  password: {{ .Values.postgres.password }}