apiVersion: plural.sh/v1alpha1
kind: Dependencies
metadata:
  application: true
  description: deploys istio operator and istio itself
spec:
  dependencies:
  - type: helm
    name: bootstrap
    repo: bootstrap
    version: '>= 0.6.14'
  - type: terraform
    name: kube
    repo: istio
    version: '>= 0.1.4'