We offer two ingress controllers, `nginx` and `internal-nginx`.  

If you'd like to only expose a service on a private endpoint, you
can usually configure the app to use the `internal-nginx` ingress class.