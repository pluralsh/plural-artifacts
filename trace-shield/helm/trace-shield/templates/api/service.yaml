apiVersion: v1
kind: Service
metadata:
  name: {{ include "trace-shield.fullname" . }}
  labels:
    {{- include "trace-shield.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "trace-shield.selectorLabels" . | nindent 4 }}
