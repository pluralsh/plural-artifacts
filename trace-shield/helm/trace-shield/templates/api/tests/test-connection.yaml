apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "trace-shield.fullname" . }}-test-connection"
  labels:
    {{- include "trace-shield.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "trace-shield.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
