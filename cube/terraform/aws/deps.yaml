apiVersion: plural.sh/v1alpha1
kind: Dependencies
metadata:
  description: cube aws setup
  version: 0.1.0
spec:
  dependencies:
  - name: aws-bootstrap
    repo: bootstrap
    type: terraform
    version: '>= 0.1.1'
  providers:
  - aws
  outputs:
    iam_access_key_id: iam_access_key_id
    iam_access_key_secret: iam_access_key_secret