registry.k8s.io:
  images-by-tag-regex:
    # the below regex can be used to limit the regex range so that only versions >= v0.8.0 are matched
    gateway-api/admission-server: (?:^v?0\.(?:[8-9]|[1-9][0-9]{1,})\.[0-9]+$)|(?:^v?(?:[1-9]|[1-9][0-9]{1,})\.[0-9]+?\.[0-9]+$)
