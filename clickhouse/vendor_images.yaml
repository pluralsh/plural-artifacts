docker.io:
  images-by-tag-regex:
    # the below regex can be used to limit the regex range so that only versions >= 22.8.0.0 are matched
    clickhouse/clickhouse-server: (?:^v?22\.(?:(?:[^0-7]{1})?(?:[8-9]{1}|[0-9]{2}))+?\.[0-9]+\.[0-9]+$)|(?:)(?:^v?(?:(?:[^0-1]{1}[0-9]?)?(?:[2-9]{1}[3-9]))+\.[0-9]+?\.[0-9]+\.[0-9]+$)
    # the below regex can be used to limit the regex range so that only versions >= 1.5.0 are matched
    altinity/clickhouse-backup: (?:^v?1\.(?:(?:[^0-4]{1})?(?:[5-9]{1}|[0-9]{2}))+?\.[0-9]+$)|(?:)(?:^v?[2-9]+\.[0-9]+?\.[0-9]+$)
    # the below regex can be used to limit the regex range so that only versions >= 0.20.0 are matched
    altinity/clickhouse-operator: (?:^v?0\.(?:(?:[^0-1]{1}[0-9]?)?(?:[2-9]{1}[0-9]))+?\.[0-9]+$)|(?:)(?:^v?[1-9]+\.[0-9]+?\.[0-9]+$)
    altinity/metrics-exporter: (?:^v?0\.(?:(?:[^0-1]{1}[0-9]?)?(?:[2-9]{1}[0-9]))+?\.[0-9]+$)|(?:)(?:^v?[1-9]+\.[0-9]+?\.[0-9]+$)
