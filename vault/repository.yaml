name: vault
description: vault deployed on plural
category: SECURITY
private: true
icon: plural/icons/vault.png
notes: plural/notes.tpl
oauthSettings:
  uriFormat: https://{domain}/ui/vault/auth/plural/oidc/callback
  authMethod: POST
