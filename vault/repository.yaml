name: vault
description: vault deployed on plural
category: SECURITY
private: false
icon: plural/icons/vault.png
notes: plural/notes.tpl
oauthSettings:
  uriFormat: https://{domain}/ui/vault/auth/oidc/oidc/callback
  authMethod: POST
