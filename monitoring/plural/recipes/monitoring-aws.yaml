name: monitoring-aws
description: Installs monitoring on an aws eks cluster
provider: AWS
dependencies:
- repo: bootstrap
  name: aws-k8s
sections:
- name: monitoring
  configuration:
  - name: loki_bucket
    type: BUCKET
    default: loki
    documentation: bucket to store loki logs data
  items:
  - type: TERRAFORM
    name: aws
  - type: HELM
    name: monitoring
