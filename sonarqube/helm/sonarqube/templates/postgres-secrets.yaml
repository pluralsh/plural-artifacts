apiVersion: v1
kind: Secret
metadata:
  name: sonarqube.plural-postgres-sonarqube.credentials.postgresql.acid.zalan.do
  labels:
{{ include "sonarqube-plural.labels" . | indent 4 }}
stringData:
  username: {{ .Values.postgres.user }}
  password: {{ .Values.postgres.password }}

