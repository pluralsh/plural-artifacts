apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "csgo.fullname" . }}-test-connection"
  labels:
    {{- include "csgo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "csgo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
