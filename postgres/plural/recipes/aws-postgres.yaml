name: aws-postgres
description: Installs postgres operator on an EKS cluster
provider: AWS
dependencies:
- repo: bootstrap
  name: aws-k8s
sections:
- name: postgres
  items:
  - type: TERRAFORM
    name: aws
    configuration:
    - name: wal_bucket
      documentation: s3 bucket to store wal archives in
      type: STRING
  - type: HELM
    name: postgres
    configuration: []