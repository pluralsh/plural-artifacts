name: gcp-postgres
description: Installs postgres operator on a GKE cluster
provider: GCP
dependencies:
- repo: bootstrap
  name: gcp-kubernetes
sections:
- name: postgres
  items:
  - type: TERRAFORM
    name: gcp
    configuration:
    - name: wal_bucket
      documentation: gcs bucket to store wal archives in
      type: STRING
  - type: HELM
    name: postgres
    configuration: []
  