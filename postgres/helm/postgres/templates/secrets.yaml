apiVersion: v1
kind: Secret
metadata:
  name: postgres-environment
  labels:
{{ include "postgres.labels" . | indent 4 }}
  annotations:
    kubed.appscode.com/sync: ""
stringData:
{{ toYaml .Values.configSecret | indent 2 }}
