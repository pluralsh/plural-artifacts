name: aws-mysql
description: Installs mysql operator on an EKS cluster
provider: AWS
dependencies:
- repo: bootstrap
  name: aws-k8s
sections:
- name: mysql
  items:
  - type: TERRAFORM
    name: aws
    configuration:
    - name: backup_bucket
      documentation: bucket to store mysql backups in
      type: STRING
  - type: HELM
    name: mysql
    configuration: []