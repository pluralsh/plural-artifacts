name: kubecost-gcp
description: Installs kubecost on a gcp gke cluster
provider: GCP
dependencies:
- repo: bootstrap
  name: gcp-kubernetes
sections:
- name: kubecost
  items:
  - type: TERRAFORM
    name: gcp
    configuration: []
  - type: HELM
    name: kubecost
    configuration: []
