name: kubecost-aws
description: Installs kubecost on an aws eks cluster
provider: AWS
dependencies:
- repo: bootstrap
  name: aws-k8s
sections:
- name: kubecost
  items:
  - type: TERRAFORM
    name: aws
    configuration: []
  - type: HELM
    name: kubecost
    configuration: []
