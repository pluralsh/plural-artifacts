{{- if .Values.viewerController.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "pipelines.fullname" . }}-viewer-controller
  labels:
    {{- include "pipelines.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "pipelines.fullname" . }}-viewer-controller
  minReplicas: {{ .Values.viewerController.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.viewerController.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.viewerController.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.viewerController.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.viewerController.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.viewerController.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
