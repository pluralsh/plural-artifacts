{{- if .Values.persistenceAgent.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "pipelines.fullname" . }}-persistence-agent
  labels:
    {{- include "pipelines.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "pipelines.fullname" . }}-persistence-agent
  minReplicas: {{ .Values.persistenceAgent.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.persistenceAgent.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.persistenceAgent.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.persistenceAgent.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.persistenceAgent.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.persistenceAgent.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
