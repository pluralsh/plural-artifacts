{{- if .Values.scheduledWorkflow.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "pipelines.fullname" . }}-scheduled-workflow
  labels:
    {{- include "pipelines.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "pipelines.fullname" . }}-scheduled-workflow
  minReplicas: {{ .Values.scheduledWorkflow.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.scheduledWorkflow.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.scheduledWorkflow.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.scheduledWorkflow.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.scheduledWorkflow.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.scheduledWorkflow.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
