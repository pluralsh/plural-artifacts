{{- if .Values.visualizationServer.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "pipelines.fullname" . }}-visualization-server
  labels:
    {{- include "pipelines.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "pipelines.fullname" . }}-visualization-server
  minReplicas: {{ .Values.visualizationServer.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.visualizationServer.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.visualizationServer.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.visualizationServer.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.visualizationServer.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.visualizationServer.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
