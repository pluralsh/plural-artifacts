{{- if .Values.metadata.writer.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "pipelines.fullname" . }}-metadata-writer
  labels:
    {{- include "pipelines.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "pipelines.fullname" . }}-metadata-writer
  minReplicas: {{ .Values.metadata.writer.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.metadata.writer.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.metadata.writer.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.metadata.writer.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.metadata.writer.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.metadata.writer.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
