apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "directus-plural.fullname" . }}-test-connection"
  labels:
    {{- include "directus-plural.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "directus-plural.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
