docker.io:
  images:
   # TODO: add external DNS and move away from forked image
    # prom/statsd-exporter: [] # TODO: check if this is actually used. 
  images-by-tag-regex:
    # the below regex can be used to limit the regex range so that only versions >= 1.5.0 are matched
    amazon/aws-efs-csi-driver: (?:^v?1\.(?:[5-9]|[1-9][0-9]{1,})\.[0-9]+$)|(?:^v?(?:[2-9]|[1-9][0-9]{1,})\.[0-9]+?\.[0-9]+$)
    # the below regex can be used to limit the regex range so that only versions >= 0.13.0 are matched
    appscode/kubed: (?:^v?0\.(?:1[3-9]|[2-9][0-9]|[1-9][0-9]{2,})\.[0-9]+$)|(?:^v?(?:[1-9]|[1-9][0-9]{1,})\.[0-9]+?\.[0-9]+$)
    # bitnami/kubectl: ^v1\.2[0-9]+?\.[0-9]+$ #TODO: doesn't seem to be used
    # bitnami/metrics-server: ^v?[0-9]+\.[0-9]+?\.[0-9]+$ # TODO: replace with official chart and image
    # the below regex can be used to limit the regex range so that only versions >= 3.25.0 are matched
    calico/ctl: (?:^v?3\.(?:2[5-9]|[3-9][0-9]|[1-9][0-9]{2,})\.[0-9]+$)|(?:^v?(?:[4-9]|[1-9][0-9]{1,})\.[0-9]+?\.[0-9]+$)
    calico/kube-controllers: (?:^v?3\.(?:2[5-9]|[3-9][0-9]|[1-9][0-9]{2,})\.[0-9]+$)|(?:^v?(?:[4-9]|[1-9][0-9]{1,})\.[0-9]+?\.[0-9]+$)
    calico/node: (?:^v?3\.(?:2[5-9]|[3-9][0-9]|[1-9][0-9]{2,})\.[0-9]+$)|(?:^v?(?:[4-9]|[1-9][0-9]{1,})\.[0-9]+?\.[0-9]+$)
    calico/pod2daemon-flexvol: (?:^v?3\.(?:2[5-9]|[3-9][0-9]|[1-9][0-9]{2,})\.[0-9]+$)|(?:^v?(?:[4-9]|[1-9][0-9]{1,})\.[0-9]+?\.[0-9]+$)
    calico/typha: (?:^v?3\.(?:2[5-9]|[3-9][0-9]|[1-9][0-9]{2,})\.[0-9]+$)|(?:^v?(?:[4-9]|[1-9][0-9]{1,})\.[0-9]+?\.[0-9]+$)
    calico/node-driver-registrar: (?:^v?3\.(?:2[5-9]|[3-9][0-9]|[1-9][0-9]{2,})\.[0-9]+$)|(?:^v?(?:[4-9]|[1-9][0-9]{1,})\.[0-9]+?\.[0-9]+$)
    calico/csi: (?:^v?3\.(?:2[5-9]|[3-9][0-9]|[1-9][0-9]{2,})\.[0-9]+$)|(?:^v?(?:[4-9]|[1-9][0-9]{1,})\.[0-9]+?\.[0-9]+$)
    # the below regex can be used to limit the regex range so that only versions >= 3.5.0 are matched
    equinix/cloud-provider-equinix-metal: (?:^v?3\.(?:[5-9]|[1-9][0-9]{1,})\.[0-9]+$)|(?:^v?(?:[4-9]|[1-9][0-9]{1,})\.[0-9]+?\.[0-9]+$)
public.ecr.aws:
  images-by-tag-regex:
    # the below regex can be used to limit the regex range so that only versions >= 2.4.0 are matched
    eks/aws-load-balancer-controller: (?:^v?2\.(?:(?:[^0-3]{1})?(?:[4-9]{1}|[0-9]{2}))+?\.[0-9]+$)|(?:^v?(?:(?:[^0-2]{1})?(?:[3-9]{1}|[0-9]{2}))+\.[0-9]+?\.[0-9]+$)
registry.k8s.io:
  images-by-tag-regex:
    # the below regex can be used to limit the regex range so that only versions >= v1.25.0 are matched
    autoscaling/cluster-autoscaler: (?:^v?1\.(?:2[5-9]|[3-9][0-9]|[1-9][0-9]{2,})\.[0-9]+$)|(?:^v?(?:[2-9]|[1-9][0-9]{1,})\.[0-9]+?\.[0-9]+$)
    # the below regex can be used to limit the regex range so that only versions >= v1.16.0 are matched
    provider-aws/aws-ebs-csi-driver: (?:^v?1\.(?:1[6-9]|[2-9][0-9]|[1-9][0-9]{2,5})\.[0-9]+$)|(?:^v?(?:[2-9]|[1-9][0-9]{1,})\.[0-9]+?\.[0-9]+$)
    # the below regex can be used to limit the regex range so that only versions >= v4.2.0 are matched
    sig-storage/csi-attacher: (?:^v?4\.(?:[2-9]|[1-9][0-9]{1,})\.[0-9]+$)|(?:^v?(?:[5-9]|[1-9][0-9]{1,})\.[0-9]+?\.[0-9]+$)
    # the below regex can be used to limit the regex range so that only versions >= v2.7.0 are matched
    sig-storage/csi-node-driver-registrar: (?:^v?2\.(?:[7-9]|[1-9][0-9]{1,})\.[0-9]+$)|(?:^v?(?:[3-9]|[1-9][0-9]{1,})\.[0-9]+?\.[0-9]+$)
    # the below regex can be used to limit the regex range so that only versions >= v3.4.0 are matched
    sig-storage/csi-provisioner: (?:^v?3\.(?:[4-9]|[1-9][0-9]{1,})\.[0-9]+$)|(?:^v?(?:[4-9]|[1-9][0-9]{1,})\.[0-9]+?\.[0-9]+$)
    # the below regex can be used to limit the regex range so that only versions >= v1.7.0 are matched
    sig-storage/csi-resizer: (?:^v?1\.(?:[7-9]|[1-9][0-9]{1,})\.[0-9]+$)|(?:^v?(?:[2-9]|[1-9][0-9]{1,})\.[0-9]+?\.[0-9]+$)
    # the below regex can be used to limit the regex range so that only versions >= v6.2.0 are matched
    sig-storage/csi-snapshotter: (?:^v?6\.(?:[2-9]|[1-9][0-9]{1,})\.[0-9]+$)|(?:^v?(?:[7-9]|[1-9][0-9]{1,})\.[0-9]+?\.[0-9]+$)
    # the below regex can be used to limit the regex range so that only versions >= v2.9.0 are matched
    sig-storage/livenessprobe: (?:^v?2\.(?:[9]|[1-9][0-9]{1,})\.[0-9]+$)|(?:^v?(?:[3-9]|[1-9][0-9]{1,})\.[0-9]+?\.[0-9]+$)
    # the below regex can be used to limit the regex range so that only versions >= v6.2.0 are matched
    sig-storage/snapshot-controller: (?:^v?6\.(?:[2-9]|[1-9][0-9]{1,})\.[0-9]+$)|(?:^v?(?:[7-9]|[1-9][0-9]{1,})\.[0-9]+?\.[0-9]+$)
    sig-storage/snapshot-validation-webhook: (?:^v?6\.(?:[2-9]|[1-9][0-9]{1,})\.[0-9]+$)|(?:^v?(?:[7-9]|[1-9][0-9]{1,})\.[0-9]+?\.[0-9]+$)
quay.io:
  images-by-tag-regex:
    # the below regex can be used to limit the regex range so that only versions >= v1.29.0 are matched
    tigera/operator: (?:^v?1\.(?:2[9]|[3-9][0-9]|[1-9][0-9]{2,})\.[0-9]+$)|(?:^v?(?:[2-9]|[1-9][0-9]{1,})\.[0-9]+?\.[0-9]+$)
    # the below regex can be used to limit the regex range so that only versions >= v1.11.0 are matched
    jetstack/cert-manager-cainjector: (?:^v?1\.(?:1[1-9]|[2-9][0-9]|[1-9][0-9]{2,})\.[0-9]+$)|(?:^v?(?:[2-9]|[1-9][0-9]{1,})\.[0-9]+?\.[0-9]+$)
    jetstack/cert-manager-controller: (?:^v?1\.(?:1[1-9]|[2-9][0-9]|[1-9][0-9]{2,})\.[0-9]+$)|(?:^v?(?:[2-9]|[1-9][0-9]{1,})\.[0-9]+?\.[0-9]+$)
    jetstack/cert-manager-webhook: (?:^v?1\.(?:1[1-9]|[2-9][0-9]|[1-9][0-9]{2,})\.[0-9]+$)|(?:^v?(?:[2-9]|[1-9][0-9]{1,})\.[0-9]+?\.[0-9]+$)
    jetstack/cert-manager-ctl: (?:^v?1\.(?:1[1-9]|[2-9][0-9]|[1-9][0-9]{2,})\.[0-9]+$)|(?:^v?(?:[2-9]|[1-9][0-9]{1,})\.[0-9]+?\.[0-9]+$)
