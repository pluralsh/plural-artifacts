apiVersion: plural.sh/v1alpha1
kind: Dependencies
metadata:
  description: Creates an AKS cluster and prepares it for bootstrapping
  version: 0.1.0
spec:
  breaking: true
  dependencies: []
  providers:
  - azure

