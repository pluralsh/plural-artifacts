# Default values for azure-identity.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

aad-pod-identity:
  mic:
    resources:
      requests:
        cpu: 50m
        memory: 100Mi