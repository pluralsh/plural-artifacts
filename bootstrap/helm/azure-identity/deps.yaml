apiVersion: plural.sh/v1alpha1
kind: Dependencies
metadata:
  description: installs the open source solution for azure active directory pod bindings
spec:
  dependencies:
  - type: terraform
    name: azure-bootstrap
    repo: bootstrap
    version: '>= 0.1.1'