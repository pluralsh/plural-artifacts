crds:
  create: true

cluster-api-provider-azure:
  crds:
    create: false
  configVariables:
    exprimental:
      machinePool: true
  controllerManager:
    manager:
      image:
        repository: ghcr.io/pluralsh/cluster-api-azure-controller
        tag: v1.9.13
