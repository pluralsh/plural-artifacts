apiVersion: platform.plural.sh/v1alpha1
kind: Proxy
metadata:
  name: alertmanager
  labels:
{{ include "bootstrap.labels" . | indent 4 }}
spec:
  type: web
  target: service/bootstrap-alertmanager
  webConfig:
    port: 9093