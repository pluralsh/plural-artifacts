apiVersion: plural.sh/v1alpha1
kind: Dependencies
metadata:
  application: true
  description: installs the aws load balancer controller
spec:
  dependencies:
  - type: terraform
    name: aws-bootstrap
    repo: bootstrap
    version: '>= 0.1.2'
