apiVersion: plural.sh/v1alpha1
kind: Dependencies
metadata:
  application: true
  description: installs the aws efs csi driver
spec:
  dependencies:
  - type: terraform
    name: aws-bootstrap
    repo: bootstrap
    version: '>= 0.1.2'
  - type: terraform
    name: aws-bootstrap
    repo: aws-efs
    version: '>= 0.1.2'
