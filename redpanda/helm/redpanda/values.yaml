# Default values for redpanda.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

redpanda:
  auth:
    sasl:
      enabled: true
    users:
      - name: { { redpanda.secrets.rootUser } }
        password: { { redpanda.secrets.rootPassword } }
