name: tier-gcp
description: Installs tier on an gcp gke cluster
provider: GCP
dependencies:
  - repo: bootstrap
    name: gcp-k8s

sections:
  - name: tier
    configuration: []
    items:
      - type: TERRAFORM
        name: kube
      - type: HELM
        name: tier
