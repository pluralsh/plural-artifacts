apiVersion: v1
kind: Secret
metadata:
  name: touca-env
  labels:
stringData:
  MINIO_USER: {{ .Values.minio.user }}
  MINIO_PASSWORD: {{ .Values.minio.password }}
  MINIO_PASS: {{ .Values.minio.password }}
  MINIO_ROOT_USER: {{ .Values.minio.user }}
  MINIO_ROOT_PASSWORD: {{ .Values.minio.password }}