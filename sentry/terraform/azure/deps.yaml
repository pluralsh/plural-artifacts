apiVersion: plural.sh/v1alpha1
kind: Dependencies
metadata:
  description: sentry azure setup
  version: 0.1.6
spec:
  dependencies:
  - name: azure-bootstrap
    repo: bootstrap
    type: terraform
    version: '>= 0.1.1'

  providers:
  - azure
