docker.io:
  images-by-tag-regex:
    # the below regex can be used to limit the regex range so that only versions >= 0.526.0 are matched
    lightdash/lightdash: (?:^v?0\.(?:52[6-9]|5[3-9][0-9]|[6-9][0-9]{2}|[1-9][0-9]{3,})\.[0-9]+$)|(?:^v?(?:[1-9]|[1-9][0-9]{1,})\.[0-9]+?\.[0-9]+$)
