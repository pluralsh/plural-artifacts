dependencies:
- name: azure-bootstrap
  repo: bootstrap
  type: terraform
  version: '>= 0.1.1'

providers:
- azure

outputs:
  console_msi_client_id: console_msi_client_id
  console_msi_id: console_msi_id

description: airflow azure setup
version: 0.1.1