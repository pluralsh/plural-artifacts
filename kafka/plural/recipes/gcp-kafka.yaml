name: gcp-kafka
description: Installs kafka on an gcp eks cluster
provider: GCP
dependencies:
- repo: bootstrap
  name: gcp-kubernetes
sections:
- name: kafka
  configuration: []
  items:
  - type: TERRAFORM
    name: kube
  - type: HELM
    name: kafka
