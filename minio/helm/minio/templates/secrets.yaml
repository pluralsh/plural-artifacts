apiVersion: v1
kind: Secret
metadata:
  name: minio-secret
  labels:
type: Opaque
data:
  access-key: {{ .Values.secret.accessKey | b64enc | quote }}
  secret-key: {{ .Values.secret.secretKey | b64enc | quote }}
  key.json: ""
