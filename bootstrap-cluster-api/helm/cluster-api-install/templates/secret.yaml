apiVersion: v1
kind: Secret
metadata:
  name: variables
  namespace: capa-system
type: Opaque
data:
{{ if eq .Values.provider "aws" }}
  AWS_REGION: {{ .Region | b64enc | quote }}
  EXP_MACHINE_POOL: {{ true | b64enc | quote }}
{{end}}
