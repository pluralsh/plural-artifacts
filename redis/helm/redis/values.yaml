redis:
  auth:
    existingSecret: redis-password
    existingSecretPasswordKey: password

  image:
    registry: gcr.io/pluralsh

  metrics:
    enabled: true
    image:
      registry: gcr.io/pluralsh
    serviceMonitor:
      enabled: true

    sentinel:
      enabled: true
      serviceMonitor:
        enabled: true
      image:
        registry: gcr.io/pluralsh
