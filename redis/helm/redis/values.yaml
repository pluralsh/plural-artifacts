redis:
  auth:
    existingSecret: redis-password
    existingSecretPasswordKey: password

  image:
    # registry: gcr.io/pluralsh
    # tag: 7.0.5-debian-11-r7 TODO: Uncomment.

  metrics:
    enabled: true
    image:
      # registry: gcr.io/pluralsh
      # tag: 1.44.0-debian-11-r16 TODO: Uncomment.
    serviceMonitor:
      enabled: true

    sentinel:
      enabled: true
      serviceMonitor:
        enabled: true
      image:
        # registry: gcr.io/pluralsh
        # tag: 7.0.5-debian-11-r6 TODO: Uncomment.
