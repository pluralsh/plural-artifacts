name: gcp-redis
description: Installs redis on a GKE cluster
provider: GCP
dependencies:
- repo: bootstrap
  name: gcp-kubernetes
sections:
- name: redis
  configuration: []
  items:
  - type: TERRAFORM
    name: kube
  - type: HELM
    name: redis
