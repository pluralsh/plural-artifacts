apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "kas.configMapName" . }}
  labels:
    {{- include "kas.labels" . | nindent 4 }}
data:
  config.yaml: |
    plural_url: "" # TODO: Add full config.
