name: kas-gcp
description: Installs kas
provider: GCP
primary: true
dependencies:
- repo: bootstrap
  name: gcp-k8s
sections:
- name: kas
  configuration:
  - name: hostname
    documentation: domain on which you'd like to host kas
    type: DOMAIN
  items:
  - type: TERRAFORM
    name: kube
  - type: HELM
    name: kas
